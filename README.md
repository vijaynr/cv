# cv (cv generator for command line)

A command line tool to convert markdown into pdf and html. The main use case of this tool is to write individual's resume in markdown and render them in pdf with user-defined style templates.

## To do list

* [ ] Design the workflow of the application
* [ ] Create the command line app to generate a pdf with default styles
* [ ] Create the command line app to generate a html with default styles
